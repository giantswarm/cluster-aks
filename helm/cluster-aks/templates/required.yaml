{{- $_ := required "global.providerSpecific.location is required" .Values.global.providerSpecific.location }}
{{- $_ := required "global.providerSpecific.subscriptionId is required" .Values.global.providerSpecific.subscriptionId }}
{{- $_ := required "global.metadata.organization is required" .Values.global.metadata.organization }}
{{- $_ := required "global.managementCluster is required" .Values.global.managementCluster }}
{{- $_ := required "global.connectivity.baseDomain is required. The values from the 'cluster-app-installation-values' ConfigMap in the default namespace should be passed when deploying this chart by adding a reference to the ConfigMap in your App custom resource. This is normally done automatically by kyverno" .Values.global.connectivity.baseDomain }}
